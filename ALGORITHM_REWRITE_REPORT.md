# 算法重写可行性报告

> 项目:B站视频监控与播放量预测系统(pyqt6 分支)
> 日期:2026-08-11
> 范围:algorithms/ 层 136 个预测算法的重写可行性专项评估
> 关联:REFACTOR_REPORT.md(全库重构报告,其中 P2-2 涉及算法骨架)

---

## 1. 结论先行

| 重写方式 | 可行性 | 工作量 | 风险 | 建议 |
|---|---|---|---|---|
| **A. 整体推倒重写 136 算法** | ❌ 不可行 | 6-10 周 | **极高** | 否决 |
| **B. 骨架模板化(行为不变)** | ✅ 可行 | 2-4 周 | 中 | **推荐首选** |
| **C. 按类别渐进重写** | ✅ 可行 | 每大类 3-5 天 | 中-高 | 推荐,先易后难 |

核心判断:**算法层不适合"重写",适合"重构"**。136 个算法不是 136 份独特逻辑——实测约 **60-70% 是重复样板**,真正的独特数学逻辑集中在一小部分文件。重写的正确姿势是:锁行为 → 抽骨架 → 收敛重复 → 逐类替换,而不是从零重写。

---

## 2. 现状量化

### 2.1 规模与构成

| 类别 | 文件数 | 行数 | predict() 均长 | 特点 |
|---|---:|---:|---:|---|
| simple | 1 | 80 | — | 单一基础算法 |
| growth | 9 | 1,628 | — | 曲线拟合(logistic/gompertz/richards/weibull) |
| time_series | 21 | 4,297 | 24 行 | ARIMA/SARIMA/Holt-Winters/卡尔曼 |
| statistical | 12 | 3,109 | **72 行** | SVR/随机森林/高斯过程/贝叶斯,predict 最长 |
| ensemble | 22 | 4,558 | 22 行 | 投票/堆叠/xgboost/lightgbm/catboost |
| deep_learning | 43 | **10,807** | 21 行 | 占算法层 **29%**,39 文件共享 _torch_upgrade |
| advanced | 20 | 4,334 | 39 行 | Hawkes/贝叶斯/生命周期,独特数学最多 |
| content / event / frequency | 9 | 1,458 | — | **三类各 3 文件,结构近乎相同** |
| **合计** | **137** | **30,271** | — | 注册 136 算法 |

### 2.2 依赖分布(137 个算法文件)

```
numpy     112 文件   ← 绝大多数算法纯 numpy + 数学
sklearn    12 文件   (SVR/随机森林/高斯过程/TSFC)
torch       8 文件   (直接 import;其余 39 个 DL 走 _torch_upgrade 间接)
scipy       3 / statsmodels 3 / xgboost·lightgbm·catboost·ngboost·prophet·uni2ts 各 1
```

→ 依赖面很窄。重写不需要处理复杂依赖矩阵,主要风险在**数值行为**,不在**依赖**。

### 2.3 重复率实测

| 重复模式 | 数量 | 分布 |
|---|---|---|
| 手写兜底块 `remaining / velocity if velocity > 0 else inf` | **79 处** | 52 文件 |
| 兜底结果 `confidence=0.3, current_views=..., current_velocity=...` | **45 处** | 33 文件 |
| `PredictionResult(...)` 构造样板 | **88 处** | 36 文件 |
| `_fallback()` 基类通用兜底(**已存在但少用**) | 49 处 | 20 文件 |
| 单文件重复:svr_predictor 同一无效结果块 | **4 次** | 1 文件 |
| content/event/frequency 三组各 3 文件 | 近乎逐行相同 | 9 文件 |

### 2.4 复杂度热点

| 位置 | 复杂度 | 影响面 |
|---|---|---|
| `_torch_upgrade.py`(2146 行 / 33 模型类 / F61) | 极差 | 全部 43 个 DL 算法 |
| `registry.py` `_prepare_video_data`(D/29)+ 21 职责 | 差 | 全部算法调用入口 |
| `base.py` `detect_surge`(E/37) | 差 | 全部算法继承 |
| `model_adapter.py` `_parse_result`(D/21,双格式分派) | 差 | 旧/新接口桥接 |

### 2.5 测试覆盖 ⚠️ 关键短板

| 测试文件 | 行数 | 覆盖 |
|---|---:|---|
| test_base_algorithm.py | 205 | 基类契约 |
| test_model_algorithms.py | 254 | 算法注册/少量算法 |
| test_models.py | 133 | 模型基础 |
| test_weight_manager.py | 127 | 权重管理 |
| **合计** | **~720** | **136 个算法中只有极小部分有行为断言** |

→ **没有行为基线,任何"重写"都无法证明等价。这是整体重写被否决的首要原因。**

---

## 3. 三种方式的可行性评估

### 方式 A:整体推倒重写(否决)

**主张**:从零重写 136 个算法,一次性生成干净代码。

**否决理由**:
1. **无等价性证明手段**:测试覆盖 ~720 行,不足以锁定 136 个算法的数值输出。重写后无法回答"预测结果是否一致"。
2. **行为依赖隐含知识**:增长曲线的 `bounds/p0/maxfev`、卡尔曼的噪声参数、Hawkes 的核函数——这些调参结果是 1 年+ 迭代积累的,重写=丢失。
3. **覆盖面与精力不匹配**:30,271 行中仅 ~30% 是"值得重写的独特逻辑",用 100% 精力重写 100% 代码,浪费 70%。
4. **训练管线耦合**:DL 算法与 trainer.py/dataset.py/checkpoint_manager 深度耦合,重写算法=重写训练系统。

**估算**:6-10 周,回归风险极高,收益为负。

### 方式 B:骨架模板化(推荐首选)

**主张**:不动算法数学,只统一 predict() 的"壳"。

**具体做法**:
1. `BaseAlgorithm` 提供**模板方法**(Template Method):
   ```python
   def predict(self, video_data, threshold, **kw):
       prepared = self._prepare(video_data)          # 数据清洗+速度计算
       if prepared is None:
           return self._fallback(...)                 # 已存在,统一兜底
       result = self._fit_predict(prepared, threshold)  # 子类只实现这步
       return self._wrap_result(result, threshold)     # 统一 PredictionResult 构造
   ```
2. 52 文件的手写兜底块 → 删除,改用 `self._fallback()`(基类已实现,49 处已在用);
3. 36 文件的 `PredictionResult` 构造样板 → 收敛到 `_wrap_result`;
4. svr_predictor 4 份重复块 → 1 份;
5. content/event/frequency 9 个近同文件 → 3 个参数化算法(配置驱动)。

**可行性**:
- 基类已具备 `_fallback/_prepare_timeseries_data/_safe_curve_fit/calculate_velocity` 等完整助手 → **基础设施已就位**;
- 大多数算法 predict() 均长 21-24 行,抽壳后子类只需保留数学核心;
- 行为不变,可用回测锁基线。

**估算**:2-4 周(含锁基线 2-3 天)。**收益**:删 ~5,000-7,000 行重复(占算法层 20-25%)。

### 方式 C:按类别渐进重写(推荐,可与 B 叠加)

按"风险从低到高"逐类处理:

| 顺序 | 类别 | 风险 | 依据 |
|---|---|---|---|
| 1 | content/event/frequency(9 文件) | **低** | 三组结构近乎相同,参数化后删 ~60% 行 |
| 2 | ensemble(22 文件) | 低 | 大多是库的薄封装(投票/堆叠/平均) |
| 3 | simple(1 文件) | 低 | 80 行 |
| 4 | time_series(21 文件) | 中 | ARIMA/卡尔曼数值敏感,需回测 |
| 5 | deep_learning(43 文件) | 中 | 重写 **_torch_upgrade 基础设施**而非 43 个模型类;33 个 nn.Module 高度同构可收敛 |
| 6 | growth(9 文件) | 中-高 | 曲线拟合 bounds/p0 调参结果需逐项保留 |
| 7 | statistical(12 文件) | 高 | predict 均长 72 行,含手工数值逻辑 |
| 8 | advanced(20 文件) | **高** | 独特数学(Hawkes/贝叶斯/生命周期),无重复可抄 |

**估算**:每大类 3-5 天,总计 3-5 周。

---

## 4. 风险清单与兜底

| 风险 | 等级 | 兜底方案 |
|---|---|---|
| 数值行为回归(最致命) | 高 | 重写前先建**特征化测试**:用固定样本数据跑 136 算法,把输出快照成基线;重写后 diff |
| 测试覆盖不足 | 高 | 用现有 `rollout_backtest.py` 回测框架做全量对比(预测 vs 实际历史) |
| 曲线拟合数值漂移 | 中-高 | growth 类保留原 `bounds/p0/maxfev` 参数,只改壳不改芯 |
| DL 训练管线耦合 | 中 | DL 只重构 `_torch_upgrade` 的推理/VRAM 编排,不动 trainer/dataset 契约 |
| sklearn/库版本行为差异 | 中 | 依赖版本已锁定(numpy 2.2.x 等),重写不升库 |
| 算法注册数漂移 | 低 | 验收标准:注册数恒为 136 |

**硬性验收标准**:
1. `AlgorithmRegistry.initialize()` 注册数 = 136(不变);
2. 特征化测试:每个算法的 predict 输出与重写前 **diff = 0**(容差 1e-6);
3. radon:E/F 级函数归零;
4. 回测 MAE/MAPE 无劣化。

---

## 5. 工作量与收益汇总

| 方式 | 工作量 | 删行 | 风险 | ROI |
|---|---:|---:|---|---|
| A. 整体重写 | 6-10 周 | ~30K 全重写 | 极高 | 负 |
| B. 骨架模板化 | 2-4 周 | 5-7K(20-25%) | 中 | **高** |
| C. 类别渐进 | 3-5 周 | 类别内 30-60% | 中-高 | 中-高 |
| B+C 组合 | 4-7 周 | 8-10K(30%) | 中 | **最高** |

---

## 6. 建议实施方案(与全库报告 P2-2 对齐)

```
第 0 步(前置,2-3 天):特征化测试锁基线
   用固定样本跑 136 算法 → 输出快照入库 → 作为重写等价性裁判

第 1 步(3-5 天):BaseAlgorithm 模板方法 + 兜底/结果构造收敛
   52 文件手写兜底 → _fallback();36 文件 PredictionResult 样板 → _wrap_result

第 2 步(1-2 天):content/event/frequency 参数化合并
   9 文件 → 3 个配置驱动算法(删 ~900 行)

第 3 步(3-5 天):_torch_upgrade.py 基础设施拆分
   33 模型类收敛(LSTM/GRU/BiLSTM 同构 → 基类+配置)+ 推理/VRAM/预处理分文件
   注:这属于"重构",模型数值不变

第 4 步(可选,2-4 周):time_series / statistical / advanced 逐类处理
   高风险类仅在特征化基线就绪后动
```

**关键原则**:
- **先锁行为,再动代码**——特征化测试是唯一的安全网;
- **只统一壳,不碰芯**——曲线拟合参数、卡尔曼参数、核函数参数一律保留;
- **DL 重写=重写基础设施**,不是重写 43 个模型类;
- 每完成一步跑一次全量验收(注册数=136 + 输出 diff=0)。

---

## 7. 一句话结论

算法层"重写"的正确打开方式不是推倒重来,而是**用模板方法统一骨架 + 收敛 9 个近同文件 + 拆分 _torch_upgrade 基础设施**,在不改变任何数值行为的前提下删掉 8-10K 行重复——**推荐 B+C 组合,前置特征化测试锁基线,分 4 步推进**。
