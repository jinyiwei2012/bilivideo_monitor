# 项目路线图 — 可增加功能与优化

> 生成日期: 2026-06-25 | 基于全项目代码审查

---

## 一、可快速实现的功能增强

### 1. 深色/亮色主题切换
- **现状**: `theme.py` 已完整定义 `THEME_LIGHT` / `THEME_DARK` 两套令牌，`apply_theme()` 已实现，但 UI **没有切换入口**
- **工作量**: ~2h — 在设置面板加开关，调用 `apply_theme(light=True/False)` 即可

### 2. 自定义播放量阈值
- **现状**: 阈值固定为 10万/100万/1000万（`ui/helpers.py:THRESHOLDS`）
- **工作量**: ~3h — 设置面板加可编辑列表，存入 `settings.json`

### 3. 系统托盘最小化
- **现状**: 关闭窗口即退出，无法后台运行
- **工作量**: ~2h — 添加 `QSystemTrayIcon`，关闭时最小化到托盘

### 4. 键盘快捷键
- **现状**: 所有操作依赖鼠标
- **建议**: `Ctrl+N` 添加 / `Ctrl+R` 刷新 / `Delete` 删除 / `Ctrl+F` 搜索 / `F11` 看板
- **工作量**: ~2h

---

## 二、数据分析功能增强

### 5. 预测准确率历史追踪
- **现状**: 预测结果写入 DB，但从未回溯对比「预测值 vs 实际值」
- **建议**: 新增「预测回看」面板 — 选择历史预测时间点，展示偏差 + 按算法排名
- **数据**: `predictions` 表已有 `predicted_seconds`, `current_views`, `timestamp`
- **工作量**: ~5h

### 6. 时段播放模式分析
- **现状**: 图表只展示时序趋势，不揭示周期性
- **建议**: 按小时/星期聚合，展示平均播放增量热力图
- **示例**: 「该视频每天 20:00-22:00 播放增量最高」
- **工作量**: ~4h

### 7. 视频对比增强
- **现状**: `data_comparison.py` 已支持趋势对比和快照对比
- **可增强**:
  - 「同期对比」— 同一天数后多个视频的播放量排名
  - 「增速排名」— 按最近 24h 播放增量排序
  - 「预测一致性」— 对比各算法对同一视频的预测分歧度
- **工作量**: ~5h

---

## 三、通知与集成

### 8. Webhook 通知（Discord / Slack / 企业微信）
- **现状**: 仅支持 Windows 原生通知 + QQ Bot (OneBot)
- **建议**: 新增 Webhook 通道，用户填入 URL，突破阈值时 POST JSON
- **工作量**: ~3h — 在 `notification.py` 中新增 `_call_action_webhook()`

### 9. 监控报告自动导出
- **现状**: `report_scheduler.py` 框架已有，但只支持手动导出
- **建议**: 添加定时导出（每日/每周），生成 CSV/JSON，可选邮件发送
- **工作量**: ~4h

---

## 四、用户体验

### 10. 启动画面 (Splash Screen)
- **现状**: 启动时窗口空白 ~1-3s（等待算法后台加载 + 首次渲染）
- **建议**: `QSplashScreen` 显示 logo + "正在加载算法…" 进度提示
- **工作量**: ~1h

### 11. 撤销删除
- **现状**: 删除监控视频后无法恢复
- **建议**: 软删除 → 30s 定时器后真删除，期间可撤销
- **工作量**: ~2h

### 12. 视频备注/标签增强
- **现状**: `tag_manager.py` 支持手动标签
- **建议**: 自动标签建议（基于标题关键词/UP主分类/播放量区间）+ 标签筛选统计
- **工作量**: ~3h

---

## 五、代码库改进（技术债）

### 13. 拆分超大文件
| 文件 | 当前行数 | 建议拆分 |
|------|:------:|------|
| `_torch_upgrade.py` | 2,148 | 按模型类型拆为 5-6 个文件 |
| `training_panel.py` | 1,537 | 拆为 `_ui.py` + `_logic.py` |
| `video_db.py` | 1,355 | 按表拆为 monitor/predictions/danmaku/weekly/yearly |
| `settings_advanced.py` | 1,033 | 按标签页拆分 |
| `finetune_panel.py` | 932 | 分离 UI 构建和训练逻辑 |
- **工作量**: ~8h

### 14. 看板模式 Widget 缓存
- **现状**: 每 15s 轮换，销毁并重建整个页面
- **修复**: 首次构建后缓存 widget 引用，翻页时只 `setText()` 更新数值
- **工作量**: ~3h

### 15. 算法 fallback 模式提取
- **现状**: 22+ 算法文件重复相同的 `_sklearn_predict` / `_numpy_predict` / `_fallback` 模式
- **建议**: 提取为 `_PredictMixin` 基类，算法只需实现核心逻辑
- **工作量**: ~6h

### 16. 静默异常添加日志
- **现状**: 30+ 处 `except Exception: pass` 在生产环境中吞没错误
- **修复**: 每处至少添加 `logger.warning()` 或 `logger.exception()`
- **工作量**: ~3h

### 17. 补充 `__init__.py`
- **现状**: `algorithms/models/` 下 10 个子目录缺少 `__init__.py`
- **工作量**: ~0.5h

---

## 优先级矩阵

| 优先级 | 项目 | 投入 | 收益 |
|:--:|------|:--:|------|
| 🔴 | #15 算法 fallback 提取 | 6h | 减少 22 文件重复代码，降低维护成本 |
| 🔴 | #13 拆分超大文件 | 8h | 提升可维护性，降低 bug 率 |
| 🟠 | #14 看板 Widget 缓存 | 3h | 消除每 15s 卡顿，即时性能提升 |
| 🟠 | #5 预测准确率追踪 | 5h | 核心功能增强，数据已有直接可用 |
| 🟠 | #1 主题切换 | 2h | 代码完成 90%，只缺 UI 开关 |
| 🟡 | #3 系统托盘 | 2h | 简单实现，显著提升体验 |
| 🟡 | #10 启动画面 | 1h | 改善第一印象 |
| 🟡 | #16 静默异常日志 | 3h | 提升可调试性 |
| 🟢 | #6 时段分析 | 4h | 差异化数据分析 |
| 🟢 | #8 Webhook 通知 | 3h | 扩展推送生态 |
| 🟢 | #17 补充 __init__.py | 0.5h | 规范化导入 |
| ⚪ | #2 自定义阈值 | 3h | 灵活度提升 |
| ⚪ | #4 快捷键 | 2h | 操作效率 |
| ⚪ | #7 视频对比增强 | 5h | 分析深度 |
| ⚪ | #9 自动导出 | 4h | 自动化 |
| ⚪ | #11 撤销删除 | 2h | 容错性 |
| ⚪ | #12 标签增强 | 3h | 组织性 |

### 🏆 「投入产出比」最高三项

1. **#1 主题切换** — 2h，代码已完成 90%
2. **#5 预测回看** — 5h，DB 数据已有，核心差异化功能
3. **#3 系统托盘** — 2h，用户体验质的飞跃

---

*与 [CODE_REVIEW.md](CODE_REVIEW.md) 配套使用*
